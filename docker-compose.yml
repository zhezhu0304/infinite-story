services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env.production
    environment:
      DATABASE_URL: "file:/app/data/prod.db"
    restart: unless-stopped
